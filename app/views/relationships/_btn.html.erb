<% if current_user != user %>
 <% if currennt_uset.follwing?(user) %>
   <%= link_to "フォロー外す", user_relationships_path(user.id),metho: :delete, class: "btn btn-info" %>
 <% else %>
    <%= link_to "フォローする", user_relationships_path(user.id),metho: :post, class: "btn btn-success" %>
 <% end %>
<% end %>
